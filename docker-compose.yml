services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=file:/app/data/cffs.db
      - AUTH_SECRET=${AUTH_SECRET:-change-me-to-a-random-secret}
    volumes:
      - cffs-data:/app/data
    restart: unless-stopped

volumes:
  cffs-data:
